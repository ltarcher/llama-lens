# 2026-08-28

## 今日完成

- LlamaLens 项目启动：完成全部调查 + v2 设计 + 3 份文档（需求/架构/UI）+ README
- 用户确认范围：本次只输出文档不写代码；后续开发以 01-需求文档.md 为主依据
- 用户关键反馈（已纳入设计）：1s 动态刷新可配置、GPU 按卡聚合分区、信息要十分详细（80+ 数据项）、高科技动态 UI、多主机 + 门户外层、90% 以上飘红

## 重要发现

- ai.lan llama-server API：/health /slots /props /v1/models 可用；/metrics 501 未启用 → token 速度必须用 /slots 差分
- /slots 字段：id_task、n_prompt_tokens(_processed/_cache)、next_token[].n_decoded/n_remain → 足够做速度差分 + 任务边界检测
- /v1/models 数据丰富：n_params 27320697856、n_embd 5120、n_vocab 248320、size 22873411584、vocab_type 2
- systemd llama-server.service 提供服务级 CPU/内存（peak）/Tasks
- 主机：i5-8500 6 核（无超线程）、31GiB、2×RTX 3080 20GB（驱动 580.159.03）、GPU1 PCIe x4
- 本机 Python 3.9.2（非 3.11）→ 代码必须 3.9 兼容；依赖全部预装（fastapi 0.115.6、paramiko 2.7.2、uvicorn 0.34.0）

## 架构决策

- 见 INDEX.md「重要架构决策」

## 未完成

- 代码开发（后端/前端）—— 待用户指示开始
- 开发后：按 AC-01~AC-13 逐项验收（含用运行中任务实测 token 速度）

## 日志解析补充（同日，用户新增需求）

- 新增 FR-13 日志解析：journalctl -u llama-server 实时 tail，解析 11 类行（正则表见架构文档 §4.4）
- 日志提供 API 拿不到的数据：tg_3s 实时速度、prompt 进度、MTP 接受率/mean len、上下文 n_tokens、KV 保留 f_keep、CUDA graphs reused、boot 信息（KV 缓存升级/MTP draft/警告）
- 架构：新增 LogPoller（专用 SSH channel 跑 journalctl -f，重连后 --since 补拉 30s）；速度来源优先级：日志 tg_3s > /slots 差分
- UI：新增"llama 实时状态区"（当前状态/上下文占用/MTP 三卡，回答"llama 当前在干嘛"）；趋势区 6→8 图（+上下文占用、MTP 接受率）；组件 15→18
- 实测关键观察（2026-08-28 18:11）：
  - 上下文跨任务持续增长：130453→...→201047（76.7% of 262144），长对话累积，截断风险 → 新增阈值 ≥80 黄 ≥90 红
  - MTP 接受率随上下文增长下降：0.931→0.688（mean len 3.79→3.06）→ 新增阈值 <80 黄 <65 红
  - prompt 速度：冷启动 ~1100 t/s（无缓存），热 ~500 t/s（KV 命中）
  - 启动日志：KV 缓存 K 自动升级 turbo3→q8_0（GQA 6:1 auto-asymmetric）、MTP draft context、n_ctx_slot 262144
- 验收标准新增 AC-14/15/16（日志解析对照 journalctl）
