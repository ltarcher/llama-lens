# LLM灵境 UI 与交互设计文档

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 日期 | 2026-08-28 |
| 上游文档 | 01-需求文档.md、02-架构设计文档.md |

## 1. 视觉风格规范（高科技深色）

### 1.1 色板

| 用途 | 颜色 | Hex | 使用 |
|---|---|---|---|
| 背景底 | 深空蓝 | #0a0e17 | 页面背景 |
| 背景渐变 | — | #0a0e17 → #0d1526 | 径向/线性渐变 + 细网格（1px 线 rgba(0,229,255,0.04)，40px 网格） |
| 卡片底 | 玻璃蓝 | rgba(16, 24, 40, 0.72) | backdrop-blur 12px |
| 卡片边框 | — | rgba(0, 229, 255, 0.15) | 1px；hover 时 0.35 |
| 主色 | 霓虹青 | #00e5ff | 主数字、激活态、主线条 |
| 辅色 | 荧光绿 | #00ff9d | 在线状态、正常值、次线条 |
| 警告 | 琥珀 | #ffc53d | 黄色阈值 |
| 危险 | 告警红 | #ff3b5c | 红色阈值、离线 |
| 文字主 | — | #e6f1ff | 标题、主数字 |
| 文字次 | — | #8fa3c8 | 标签、辅助信息 |
| 文字弱 | — | #5a6b8c | 时间戳、占位 |
| 图表网格 | — | rgba(143, 163, 200, 0.12) | — |

### 1.2 字体

- 数字/数据：'JetBrains Mono', 'Roboto Mono', monospace（tabular-nums）
- 中文/UI：system-ui, 'PingFang SC', 'Microsoft YaHei'
- 层级：条卡大数字 24px / 仪表中心数字 20px / 正文 13px / 辅助 11px

### 1.3 效果与动效

| 效果 | 规格 |
|---|---|
| 卡片 | 玻璃拟态：rgba 底 + backdrop-blur(12px) + 1px 边框 + 圆角 10px；hover 边框提亮 + translateY(-1px) |
| 发光 | 主元素 box-shadow: 0 0 12px rgba(0,229,255,0.35)；红色告警: 0 0 16px rgba(255,59,92,0.5) |
| 状态点 | 8px 圆点；在线：绿色 + 脉冲（scale 1→1.6 opacity 1→0，1.5s infinite）；离线：红色实心 |
| 数字动画 | 数值变化：300ms count-up（rAF） |
| 图表 | ECharts animationDuration 300，areaStyle 渐变（主色 0.25→0），线条发光（shadowBlur 8） |
| 飘红脉冲 | danger 态：卡片边框 + 数字变红，边框发光 1.2s infinite 脉冲 |
| 扫描线 | 页面级细微扫描线（2px 渐变条，8s 循环，opacity 0.03）——可选，可关闭 |
| 事件流 | 新事件顶部滑入（200ms），自动滚动到底（若用户位于底部） |

## 2. 门户页（/）

### 2.1 布局

```
┌────────────────────────────────────────────────────────────────┐
│ ◉ LLM灵境          主机 3 · 在线 2 · Token 速度 42.1 tok/s    │  BrandBar 56px
├────────────────────────────────────────────────────────────────┤
│ ┌──────────────────────┐  ┌──────────────────────┐             │
│ │ ● AI 主机 (ai.lan)   │  │ ● 主机2 (10.0.0.2)   │   Grid:     │
│ │ Qwen3.8-27B-Q6_K     │  │ Llama-3.1-8B         │   auto-fill │
│ │ 27.32B 参数          │  │ 8.0B 参数            │   minmax(360px,1fr)│
│ │                      │  │                      │             │
│ │ 42.3 tok/s  ~~~spark │  │ 128.5 tok/s ~~~spark │             │
│ │ GPU0 ▓▓▓▓▓▓▓░ 87%   │  │ GPU0 ▓▓▓░░░░░ 32%   │             │
│ │ GPU1 ▓▓▓▓▓▓▓▓ 92%   │  │ (单卡)               │             │
│ │ CPU 12%  MEM 19%    │  │ CPU 45%  MEM 38%    │             │
│ └──────────────────────┘  └──────────────────────┘             │
└────────────────────────────────────────────────────────────────┘
```

### 2.2 HostCard 字段

| 区域 | 内容 | 数据源 |
|---|---|---|
| 顶部 | 状态点 + 主机名 + id | /api/hosts |
| 模型 | 模型名（短名）+ 参数量 | model.name / n_params |
| 速度 | gen_speed_tps 大数字 + 60s sparkline | gen_speed_tps + history |
| GPU | 每卡一条：利用率% + 显存% | gpus[] |
| 底部 | CPU% / MEM% / 磁盘% | cpu_pct / mem_pct |
| 告警 | 有 danger 告警时：红边框 + 右上角红色角标（数量） | alerts |

### 2.3 交互

- 卡片 hover：边框提亮、translateY(-2px)
- 点击：进入 /host/:id
- 刷新：WS /ws/portal 1s 推送
- 离线卡片：置灰 + 红色"离线"角标

## 3. 详情页（/host/:id）

### 3.1 总体布局（1920×1080 基准）

```
┌ TopBar (56px) ────────────────────────────────────────────────────────────┐
│ ← 门户 | ai.lan · Qwen3.8-27B-Q6_K | 速度 53.3t/s | MTP 83.3% | 上下文 224k/262k │
│ GPU0 48% 58° 223W 16.9/20G | GPU1 … | 内存 10.1/31.3G | CPU 18% | ● 在线 | WS 实时 ▾ │
├────────────────────────────────────────────────────────────────────────────┤
│ 实时总览区 (4 卡, 170px) —— 3 条卡 + 1 仪表                                          │
│ ┌Token 生成速度─┐ ┌预填充速度─────┐ ┌上下文占用─────┐ ┌MTP 接受率─────┐ │
│ │ 42.3 tok/s    │ │ 123.4 tok/s   │ │ 85,106        │ │  ◜ 69.8% ◝  │ │
│ │ [条]          │ │ [条]          │ │ [条]          │ │ (仪表)       │ │
│ │ [sparkline]   │ │ [sparkline]   │ │ [sparkline]   │ │ [sparkline]  │ │
│ └───────────────┘ └───────────────┘ └───────────────┘ └──────────────┘ │
├────────────────────────────────────────────────────────────────────────────┤
│ GPU 区 (按卡聚合, 两卡并排) —— 位于实时生成任务区之上（GPU 更受关注）          │
│ ┌─ GPU 0 · RTX 3080 ──────────────┐ ┌─ GPU 1 · RTX 3080 ──────────────┐   │
│ │ 环形仪表 87%    显存条           │ │ 环形仪表 92%    显存条           │   │
│ │ 18.4/20 GiB (90%)               │ │ 17.6/20 GiB (86%)               │   │
│ │ 54°C  141.5W/280W  风扇 60%     │ │ 49°C  137.3W/280W  风扇 55%     │   │
│ │ 图形 1440MHz  显存 1188MHz       │ │ 图形 1425MHz  显存 1188MHz       │   │
│ │ PCIe gen3 x16 · P2 · 驱动 580.159.03 │ PCIe gen3 x4 · P2 · 驱动 580.159.03│  │
├────────────────────────────────────────────────────────────────────────────┤
│ 实时生成任务区 (2 卡, 均固定高 320px) —— 左: "llama 当前在干嘛" / 右: 事件流(可滚动) │
│ ┌当前状态 (640px)───────────────────────┐ ┌事件流 (1fr, fill)────────────┐  │
│ │ ● 生成中 (decoding) · 任务 #14847     │ │ 15:53:10 [INFO] llama 上线…  │  │
│ │ 已解码 2843 · 实时 28.5 t/s (3s窗)    │ │ 16:02:33 [INFO] 任务开始…    │  │
│ │ 任务均值 26.2 t/s · MTP 83.3%        │ │ 16:41:07 [INFO] 任务结束…    │  │
│ └───────────────────────────────────────┘ └────────────────────────────┘  │
│ │ 进程: llama-server 18414MiB      │ │ 进程: llama-server 17626MiB      │   │
│ └──────────────────────────────────┘ └──────────────────────────────────┘   │
├────────────────────────────────────────────────────────────────────────────┤
│ 系统区 (4 卡, 200px)                                                        │
│ ┌CPU──────────────┐ ┌内存────────────┐ ┌磁盘────────────┐ ┌网络──────────┐  │
│ │ 12.3% (大数字)  │ │ 5.8/31 GiB    │ │ / 70%         │ │ eth0         │  │
│ │ 6 核竖条 ×6     │ │ used 条       │ │ 条 801G/1.2T  │ │ rx 0.5MB/s   │  │
│ │ load 1.09/0.75/ │ │ buff/cache 25G│ │ R 1.2 W 0.3   │ │ tx 0.2MB/s   │  │
│ │ 0.58  3.0GHz    │ │ swap 0/8Gi    │ │               │ │ [迷你曲线]   │  │
│ └──────────────────┘ └───────────────┘ └───────────────┘ └──────────────┘  │
├────────────────────────────────────────────────────────────────────────────┤
│ 进程区 (280px)                                                              │
│ ┌llama-server 进程卡 (2/3 宽)──────────────┐ ┌Top CPU (1/3)──────┐         │
│ │ PID 31255 | 实时CPU 45.2% | 累计 98.6%   │ │ llama-server 98.6% │        │
│ │ RSS 5.1G | VSZ 12G | 线程 10 | 运行 02:28│ │ ...                │         │
│ │ 服务 unit·active / 启动于 / CPU / 内存 / Tasks│ │ (8 行)          │         │
│ │ [命令行折叠 ▾]                           │ ├───────────────────┤         │
│ │ -m ... --n-gpu-layers all -ts 24,20 ...  │ │ Top 内存 (8 行)   │         │
│ │ [参数表: 20 项解析参数]                   │ └───────────────────┘         │
│ └──────────────────────────────────────────┘                               │
├────────────────────────────────────────────────────────────────────────────┤
│ 模型与 Slot 区 (240px)                                                      │
│ ┌模型信息 (1/2)──────────────────┐ ┌Slot 详情 (1/2)──────────────────┐     │
│ │ 路径 /share/AI/.../Qwen3.8-... │ │ Slot 0 · 运行中 · 任务 #5199    │     │
│ │ Q6_K · 27.32B · 22.87GB       │ │ prompt 19032 (已处理626/缓存0)  │     │
│ │ embd 5120 · vocab 248320      │ │ 已解码 795 · 剩余 31205         │     │
│ │ vision+video · ctx 262144     │ │ 上下文 7.6% [条]                │     │
│ │ mmproj 889MB · llamacpp       │ │ 采样: temp 0.3 top_k 40 top_p .9│     │
│ │ [全参数表折叠]                  │ │ max_tokens 32000 · spec draft-mtp│    │
│ └────────────────────────────────┘ │ [全量采样参数表折叠]            │     │
│                                   └─────────────────────────────────┘     │
├────────────────────────────────────────────────────────────────────────────┤
│ 趋势区 (12 图 3 组, 每图 170px, 窗口切换 5m/15m/1h)                          │
│ [llama] ┌Token 生成速度┐ ┌预填充速度─┐ ┌上下文占用 (1s线+n_ctx参考线)┐ ┌MTP (阶梯)┐ │
│ [GPU]   ┌利用率────┐ ┌显存────────┐ ┌温度────────┐ ┌功耗────────┐ │
│ [系统]   ┌CPU───────┐ ┌内存────────┐ ┌网络 (rx/tx)┐ ┌负载均值 (1/5/15)┐ │
└────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 TopBar

| 元素 | 说明 |
|---|---|
| 返回按钮 | ← 门户（router 回 /） |
| 主机标识 | name + 模型短名（basename，悬停 title 显示完整路径） |
| 核心指标芯片 | 单行紧凑数字芯片（空间不足时横向滚动）：速度（t/s，prompt 活跃时优先显示，tooltip 含 gen/prompt/来源）、MTP 接受率 %、上下文 剩余/总量（k）、每 GPU 利用率/温度/功耗/显存已用/总量、内存 已用/总量、CPU %；按 alerts 着色（warn 黄 / danger 红），无数据显示 — 置灰 |
| 状态徽章 | ● 在线（绿脉冲）/ llama 离线（红）/ SSH 断开（黄） |
| 刷新控制 | 下拉：实时（WS）/ 1s / 2s / 5s / 暂停；当前模式指示（WS 绿点 / 轮询黄点 / 暂停灰） |

### 3.3 实时总览区（3 条卡 + 1 仪表）

| 卡片 | 形态 | 数据 | 辅助（标题行右） | 底行 | 颜色 |
|---|---|---|---|---|---|
| Token 生成速度 | BarCard 条 | llama.gen_speed_tps | 来源 log/API；离线时"数据截至 HH:MM:SS" | 任务均速（log.state.tg_tps）+ 60s 峰/谷/均 | 离线置灰 |
| 预填充速度 | BarCard 条 | 预填充中实时值；停止后归 0（最近一次预填充信息见 sub"上次 HH:MM:SS"与 foot token 数/进度） | 同上；非预填充期显示"上次 HH:MM:SS" | 预填充中：进度 % + 预计剩余（ETA=(1-进度)×总量÷速度），且条位切换为进度条（绿色，刻度 0/50/100%）；非预填充：prompt 总 token 数 + 进度 % + 60s 峰/谷/均（全零窗口隐藏） | 离线置灰 |
| 上下文占用 | BarCard 条（0–ctx.total） | ctx.used 原始 token 数（千分位，如 85,106） | 百分比 · 剩（32.5% · 剩 177k） | 60s 峰/谷/均（k 格式） | alerts `ctx` 级别；truncated 显示红色"已截断"徽章 |
| MTP 接受率 | GaugeCard 仪表 | mtp.acceptance × 100 | spec_type×spec_draft_n_max · accepted/generated · mean len | 60s 峰/谷/均 | 值驱动颜色（<65 红 / 65-80 黄 / ≥80 绿，弧/指针/锚点/中心数字/60s 趋势全跟随）+ alerts `mtp` 级别 |

- 条卡形态：大数字（24px count-up）+ 横向条 + 0/中值/满值刻度 + 60s sparkline（1.2px 恒定线宽）+ 底行 60s 峰/谷/均
- 条满量程：速度卡动态（60s 峰值 × 1.1 取整到 1/2/2.5/5×10^n，下限 10）；上下文卡固定 ctx.total（刻度 0/中值/满值用 k 格式）
- 仪表形态：ECharts gauge（240° 弧、12px 轨道、阈值色带、主/次刻度、细指针 + 锚点），中心大数字 300ms count-up
- MTP 色带反向（0-65 红 / 65-80 黄 / 80-100 绿，低为差）；值驱动颜色由 zoneColors 提供（与色带阈值同源）
- 无数据（llama 离线 / 无任务）：条归零、数字 "—" 置灰、趋势隐藏
- 网格：>1500px 4 列；≤1500px 2 列；≤640px 1 列

### 3.4 实时生成任务区（LlamaStateCard + EventFeed）

- 两列布局（640px + 1fr），两卡均固定高 320px（`.task-row > * { height: 320px }`）：左 LlamaStateCard 回答"llama 当前在干嘛"（字段规格见 §3.5），内容超出时卡内滚动（overflow-y: auto）；右 EventFeed（fill 模式，见 §3.11）
- 固定高度目的：阶段切换（空闲/预填充/生成中）内容高度不同，避免布局跳动
- ≤1500px 回落 1 列（两卡仍各固定 320px）

### 3.5 LlamaStateCard（当前状态卡）

- 头部：阶段徽章 + 日志实时徽章 + 右侧"已运行"（now - state.started_at，随快照每秒跳动）
- 三列大数字 stat 格（stat3，动态值）：
  - 生成中：已解码（递增）/ 实时速度 3s（波动）/ 任务均速
  - Prompt 处理：Prompt 速度 / 已处理 Prompt（n_prompt_tokens_processed，递增）/ 已耗时（prompt_elapsed_s）
- 进度条：预填充 = prompt 进度；生成中 = n_decoded/(n_decoded+n_remain)（仅 n_remain>0 即设了上限时显示）
- 生成中：预计完成 ETA（n_remain ÷ tg_3s，仅 n_remain>0）+ 两列 kv（上下文占用 / MTP 接受率（log.mtp，task_end 更新，<65 红 / 65-80 黄 / ≥80 绿，附 accepted/generated）/ 剩余 tokens（n_remain，-1 显示"不限"）/ Prompt 总量 / 缓存命中 / Graphs 复用（log.graphs_reused））
- Prompt 处理：进度条 + 两列 kv（Prompt 总量 / 预计剩余 ETA / 上下文占用 / KV 缓存命中）
- 空闲：上下文占用 + 上一任务摘要（tokens/均速/MTP）+ 两列 kv（总耗时 / 预填充耗时@速度 / 生成耗时 / Graphs 复用）
- 底部配置条（cfg-strip，来自 process.flags 静态配置）：Spec 解码（spec_type×spec_draft_n_max）/ KV 缓存（cache_type_k/v）/ 批大小（batch/ubatch）/ GPU 层数
- 任务 #id、is_child、slot id/总数（活跃 slot / 总 slot 数）
- 数据：log.state.*（含 started_at）/ log.context（used/total/pct）/ log.mtp（acceptance/accepted/generated）/ log.graphs_reused / 活跃 slot（is_processing：id、n_prompt_tokens、n_prompt_tokens_cache → 缓存命中 = cache/total、n_prompt_tokens_processed、n_remain）/ process.flags
- 生成中展示的 MTP 接受率为上一任务值（log.mtp 仅 task_end 更新，与总览仪表同源）

### 3.6 GPU 面板（每卡一个，GpuPanel）

| 区域 | 内容 | 阈值 |
|---|---|---|
| 头部 | GPU 芯片图标（青色发光）+ GPU {index} · {name} | — |
| 左侧 | 环形仪表（88px 紧凑）：利用率 %（0-100） | ≥80 黄，≥90 红 |
| 右上 | 显存条：used/total GiB + % | ≥85 黄，≥95 红 |
| 右侧 | 3×4 紧凑网格（kv 11px）：温度 / 显存温度 / 功耗（draw/limit）/ 显存利用率 / 风扇 / 频率（图形/显存）/ PCIe（gen x width）/ P-State / 降频状态（位掩码→中文短名，0=正常）/ ECC（纠错/不可纠）/ CUDA 版本 / 驱动版本 | 温度 ≥75 黄 ≥85 红；功耗 ≥85% 黄 ≥95% 红；ECC 不可纠 >0 红；降频激活黄 |
| 底部 | 占用进程列表（pid name mem） | — |

### 3.7 系统区

- CpuPanel：整机 % 大数字 + 6 根竖条（每核，高度=利用率，颜色青→红渐变）+ load 1/5/15 + 主频；阈值 ≥80 黄 ≥90 红
- MemPanel：used/total 大数字 + 三条（used / buff_cache / available 堆叠）+ swap；阈值 ≥85 黄 ≥95 红
- DiskPanel：每挂载点一条（mount、used/total、%）+ 读写速率；阈值 ≥80 黄 ≥90 红
- NetPanel：每网卡 rx/tx 速率 + 60s 迷你曲线

### 3.8 进程区

- LlamaProcessCard：
  - 第一行：4 列指标网格（7 项，4+3 排布）：PID | 实时 CPU%（大数字）| 累计 CPU% | RSS | VSZ | 线程数 | 运行时长
  - 第二行：systemd 键值列表（标签左、值右，5 行）：服务 unit · active / 启动时间 / 服务级 CPU 累计 / 服务级内存（含峰值）/ Tasks
  - 命令行：等宽字体，默认展开（pre-wrap，可点击折叠）
  - 参数表：三列网格（参数名 → 值），20+ 项，可折叠（默认展开）
- TopProcessTable：两个表（Top CPU / Top 内存），各 8 行：pid name cpu% mem% rss（PID 与数值列右对齐，进程名超长省略号截断，悬停显示全名）

### 3.9 模型与 Slot 区

- ModelInfoCard：键值网格（名称/路径/ftype/参数量/文件体积/mmproj/嵌入维度/词表/上下文/模态/capabilities/owned_by）
- SlotTable：
  - 头部：Slot {id} · {运行中/空闲} · 任务 #{id_task}
  - prompt tokens：总 / 已处理 / 缓存（3 个数字）
  - 已解码 / 剩余 / 上下文占用条
  - 日志派生：prompt 进度（log）、任务耗时汇总（prompt/解码/总耗时与速度）、MTP 接受率、KV 保留 f_keep
  - 采样参数表（可折叠，默认显示 temperature/top_k/top_p/min_p/max_tokens/chat_format/speculative.types，全量 30+ 项可展开）

### 3.10 趋势区（TrendChart × 12，3 组）

12 图分 3 组（每组一行跨 2 列细标签，相关指标相邻）：

| 组 | 图表 | 序列 | 类型 |
|---|---|---|---|
| llama | Token 生成速度 | gen_speed（青） | 折线 + 面积渐变 |
| llama | 预填充速度 | prompt_speed（绿） | 折线 + 面积渐变 |
| llama | 上下文占用 | ctx_used（青，1s 采样）+ n_ctx 上限（红虚线参考线） | 折线 + 参考线 |
| llama | MTP 接受率 | 每任务接受率（绿，阶梯线，y 轴 0-100%） | 阶梯线 |
| GPU | GPU 利用率 | GPU0（青）、GPU1（绿） | 折线 |
| GPU | GPU 显存 | GPU0、GPU1（GiB） | 折线 |
| GPU | GPU 温度 | GPU0、GPU1（°C） | 折线 |
| GPU | GPU 功耗 | GPU0、GPU1（W） | 折线 |
| 系统 | CPU | 整机（青） | 折线 + 面积 |
| 系统 | 内存 | used（青）+ buff_cache（蓝，堆叠） | 堆叠面积 |
| 系统 | 网络 | rx（青）、tx（绿） | 折线 |
| 系统 | 负载均值 | load_1（青）、load_5（绿）、load_15（黄） | 折线 |

- 通用：窗口切换 5m/15m/1h（趋势区右上角全局切换）；y 轴自适应；tooltip 显示全部序列值 + 时间戳；断线时曲线断点（connectNulls: false）

### 3.11 事件流（EventFeed）

- 位置：实时生成任务区右卡（fill 模式，固定高 320px，feed-box flex:1 + overflow-y: auto，内容超出出现滚动条）；≤1500px 回落 1 列时仍固定 320px
- 终端风格：等宽字体、深色底、级别标签着色 [INFO] 绿 / [WARN] 黄 / [ERROR] 红
- 事件来源：后端 EventDetector（以日志行为主）：任务开始（任务 ID/prompt token 数）、预填充开始（prompt 总 token 数）、任务结束（总 token/耗时/平均速度/MTP 接受率/上下文占用）、阈值穿越（alert：指标 值 ≥/≤ 阈值（警告/危险）/ 恢复正常，同一 metric 两次事件最小间隔 30s）、启动（模型加载完成/boot 信息）、上下线
- 自动滚动：用户位于底部时跟随新事件；上滚后显示"↓ 新事件"按钮
- 保留 200 条，更早的淡出

## 4. 组件清单

| 组件 | 文件 | 职责 | 关键 Props/数据 |
|---|---|---|---|
| BrandBar | components/BrandBar.vue | 门户顶栏 | hosts_total, online_count |
| HostCard | components/HostCard.vue | 门户主机卡片 | host 快照摘要 |
| TopBar | components/TopBar.vue | 详情顶栏 | host, model, stats（速度/MTP/上下文/GPU/内存/CPU）, status, 刷新控制 |
| BarCard | components/BarCard.vue | 总览条卡（大数字 + 条 + 刻度 + 60s 趋势 + 峰/谷/均） | title, value, unit, digits, level, barMax, spark[], sub, badge, foot, fmt, fmtCompact |
| GaugeCard | components/GaugeCard.vue | 总览仪表卡（阈值色带 + 刻度 + 指针 + 60s 趋势 + 峰/谷/均） | title, value, unit, level, sub, spark[], foot, zones, zoneColors |
| LlamaStateCard | components/LlamaStateCard.vue | 当前状态卡（阶段/进度/速度/上下文/缓存命中/ETA） | log.state, log.context, slots |
| GpuPanel | components/GpuPanel.vue | 单 GPU 面板 | gpu 对象 |
| CpuPanel | components/CpuPanel.vue | CPU 面板 | cpu 对象 |
| MemPanel | components/MemPanel.vue | 内存面板 | mem 对象 |
| DiskPanel | components/DiskPanel.vue | 磁盘面板 | disk 对象 |
| NetPanel | components/NetPanel.vue | 网络面板 | net 对象 |
| LlamaProcessCard | components/LlamaProcessCard.vue | 进程卡 | process, service |
| TopProcessTable | components/TopProcessTable.vue | Top 进程表 | rows, mode(cpu/mem) |
| ModelInfoCard | components/ModelInfoCard.vue | 模型信息 | model |
| SlotTable | components/SlotTable.vue | Slot 详情 | slot |
| TrendChart | components/TrendChart.vue | 通用趋势图 | series[], window |
| EventFeed | components/EventFeed.vue | 事件流（任务区右卡，fill 模式） | events[], fill |

（共 17 个组件，以此表为准）

## 5. 交互规格

| 交互 | 规格 |
|---|---|
| 刷新控制 | TopBar 下拉：实时（WS）/ 1s / 2s / 5s / 暂停；模式指示点：WS 绿 / 轮询黄 / 暂停灰；暂停时停止数据请求，页面保留最后数据 + "已暂停"水印 |
| 窗口切换 | 趋势区全局切换 5m/15m/1h，所有图表同步 |
| Hover | 图表 tooltip（全部序列 + 时间戳）；卡片 hover 边框提亮；进度条 hover 显示精确值 |
| 折叠 | 命令行、参数表、采样参数表：点击标题展开/收起，200ms 高度动画 |
| 事件流 | 自动滚动 + "新事件"按钮（见 3.9） |
| 导航 | 门户卡片点击 → 详情；详情 ← 返回 → 门户（保留滚动位置） |
| 数字动画 | 数值变化 300ms count-up；大数字 font-variant-numeric: tabular-nums 防抖动 |

## 6. 阈值飘红规格

| 级别 | 数字颜色 | 卡片边框 | 附加效果 |
|---|---|---|---|
| normal | #e6f1ff | rgba(0,229,255,0.15) | — |
| warn（黄） | #ffc53d | rgba(255,197,61,0.4) | — |
| danger（红） | #ff3b5c | rgba(255,59,92,0.6) + 发光 0 0 16px rgba(255,59,92,0.5) | 边框脉冲 1.2s infinite；门户卡片右上角红色角标 |

- 评估在后端完成（snapshot.alerts[]），前端按 level 渲染
- 多指标同时超阈值：各指标独立；卡片取最高级别

## 7. 降级与空态

| 状态 | 展示 |
|---|---|
| llama 离线 | TopBar 红色徽章"llama 离线"；总览区速度卡 "—" 置灰 + "数据截至 HH:MM:SS"（sub 行）；GPU/系统区正常（SSH 仍可用） |
| SSH 断开 | TopBar 黄色徽章"SSH 断开"；GPU/系统/进程区显示"数据不可用（SSH 断开）"占位（图标 + 文字），保留最后值置灰；实时总览/生成任务区正常（API 数据仍可用） |
| 日志不可用（SSH 断开 / unit 名不符） | 实时生成任务卡显示"日志不可用，使用 API 数据"置灰；速度回退 /slots 差分并标注数据来源 API |
| 两者都断 | 全页红色横幅"主机不可达"，所有区域占位 |
| 无 GPU | GPU 区显示"未检测到 NVIDIA GPU" |
| 单 GPU | GPU 区单卡占满整行 |
| 多 Slot | Slot 区每 slot 一张卡（网格） |
| 无进程 | 进程卡显示"未找到 llama-server 进程（可能以其他名称运行）" |
| 首次加载 | 骨架屏（卡片轮廓微光），WS 首帧到达后填充数据 |

## 8. 响应式

- 基准 1920×1080；内容全宽铺满（无 max-width 限制），任意宽度无横向滚动
- 详情页断点：

| 断点 | 总览区 | 任务区 | GPU | 系统 | 模型/Slot | 进程 | 趋势 |
|---|---|---|---|---|---|---|---|
| >1500px | 4 列 | 2 列（640px+1fr） | 2 列 | 2 列 | 2 列 | 2:1 | 2 列 |
| ≤1500px | 2 列 | 1 列 | 1 列 | 2 列 | 1 列 | 1 列 | 2 列 |
| ≤1100px | 2 列 | 1 列 | 1 列 | 1 列 | 1 列 | 1 列 | 1 列 |
| ≤640px | 1 列 | 1 列 | 1 列 | 1 列 | 1 列 | 1 列 | 1 列 |

- 门户页：auto-fill minmax(360px, 1fr)，<360px 单列
- sparkline 使用 vector-effect: non-scaling-stroke，卡片拉伸时线宽恒定（总览卡 1.2px / 门户卡 1.5px）
